[package]
name = "jolt-wasm-ir"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Register-machine IR and address-space layout shared by the Jolt WebAssembly frontend and backend"
repository = "https://github.com/a16z/jolt"
keywords = ["SNARK", "zkvm", "wasm"]
categories = ["cryptography"]

[lints]
workspace = true

[dependencies]
